<div align="center"> 
<h1> Food Manager</h1>
<h2 style="border-bottom: none;"><br>
<div align="start">
3. Design<br>
</div>
<br>
22211994,남재민,njm739@gmail.com</h2>
<img width=auto height=auto alt="image" src="image/Food Manager img.png"/>
<br>
<br>
<br>
<br>
<br>
<br>
<h2 style="border-bottom: none;">
[ Revision history ]

| Revision date | Version # | Description | Author |
| :---: | :---: | :---: | :---: |
| 2026-06-01 | 1.0.0 | Initial Concept |Nam Jae-Min |
</h2>

<br><br><br><br><br><br>
<h2 style="border-bottom: none;">
= Contents =
<pre>
<span style="font-size: 18pt;">
1. Introduction ...........................................................
<br>
2. Class diagram ..........................................................
<br>
3. Sequence diagram .......................................................
<br>
4. State machine diagram ..................................................
<br>
5. Implementation requirements ............................................
<br>
6. Glossary ...............................................................
<br>
7. References .............................................................
<br>
</span>
</pre>
</h2>
</div>
<br><br><br><br><br><br><br><br>

<h2>
1. Introduction
</h2>
<div style="line-height: 1.6;">
<span style="font-size: 12pt;">
본 문서는 Analysis Document의 다음 문서인 Design 단계의 문서로, 본 시스템에 대한 세 번째 문서이다. 
<br>
1) Summary
<br>
일상생활에 바쁜 현대인들은 냉장고나 팬트리에 식료품을 저장한다. 하지만 너무 바쁜 생활로 인해서 구매하였던 식료품을 까맞게 잊고, 다시 구매하거나 소비기한이 지나서 모르고 먹어서 식중독에 걸리수도 있다. 또 상한음식으로 인해 먹지도 못하고 음식물 쓰레기가 되어버리기도 한다. 판매자입장에서도 소비기한이 얼마 남지않은 식료품의 재고가 많이 남은경우 처리하기에 매우 까다롭다. 
<br>
그래서 본 문서에서는 사용자가 앱에 식료품의 정보(소비기한,갯수,등)를 등록하고 그것을 자동으로 관리해주고 알림을 보내주어서 바쁜 현대인들의 삶에 지장이 가지않도록 하는 것이 목적이다.
<br>
2) Diagram
<br>
이번 문서에서는 프로그램에 대한 Anaylsis문서를 바탕으로 Class diagram, Sequence Diagram,State Machine Diagram을 만들고 설명할 것이다.
<br>
3) Important Points of Design
<br>
- 외부 API 및 DB의 사용 및 안정성 설계
<br>
- 소비자, 판매자의 권한을 명확히 구분하고, 각각의 데이터의 일관성을 유지해야함.
<br>
</span>
</div>
<br><br><br><br><br><br><br><br><br><br>

<h2>
2. Class Diagram
</h2>
<br>
<img width=auto height=auto alt="image" src="image/Class diagram/Class Diagram1.png"/>
<br><br>
<div style="line-height: 1.6;">
<span style="font-size: 12pt;">

### 1) User

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- id: Long` | 사용자를 구분하는 고유 번호 |
| `- userId: String` | 사용자가 로그인할 때 입력하는 아이디 |
| `- passwordHash: String` | 암호화되어 저장된 사용자 비밀번호 |
| `- name: String` | 사용자 이름 |
| `- role: String` | 사용자 유형을 나타내는 값으로 소비자 또는 판매자를 구분 |
| `- notificationEnabled: boolean` | 알림 기능 사용 여부 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ User(id: Long, userId: String, passwordHash: String, name: String, role: String, notificationEnabled: boolean)` | 사용자 객체를 생성하고 기본 정보를 설정 |
| `+ getId(): Long` | 사용자 고유 번호를 반환 |
| `+ getPasswordHash(): String` | 암호화된 비밀번호 값을 반환 |
| `+ isConsumer(): boolean` | 사용자가 소비자인지 확인 |
| `+ isSeller(): boolean` | 사용자가 판매자인지 확인 |
| `+ addFood(food: Food): void` | 사용자가 식료품을 등록 |
| `+ updateFood(food: Food): void` | 사용자가 식료품 정보를 수정 |
| `+ deleteFood(foodid: Long): void` | 사용자가 식료품을 삭제 |
| `+ viewFoods(): List<Food>` | 사용자가 등록한 식료품 목록을 조회 |
| `+ setNotification(setting: NotificationSetting): void` | 사용자의 알림 설정을 변경 |
| `+ registerAddress(address: Address): void` | 사용자의 주소를 등록 |
| `+ updateAddress(address: Address): void` | 사용자의 주소를 수정 |
| `+ receiveNotification(notification: Notification): void` | 사용자에게 알림을 전달 |

#### 2) Consumer

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| 없음 | `User`의 속성을 상속받아 사용 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ Consumer(id: Long, userId: String, passwordHash: String, name: String, notificationEnabled: boolean)` | 소비자 객체를 생성하고 사용자 기본 정보를 설정 |
| `+ viewSaleInfo(sales: List<Sale>): List<Sale>` | 소비자가 주변 할인 정보를 조회 |
| `+ recommendRecipe(foods: List<Food>): List<Recipe>` | 보유 식료품을 기준으로 추천 레시피를 조회 |

### 3) Seller

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- businessName: String` | 판매자의 사업장 이름 |
| `- businessOwnerName: String` | 사업자 대표자 이름 |
| `- businessNumber: String` | 사업자 등록번호 |
| `- businessCategory: String` | 사업장 유형으로 마트 또는 편의점 등을 구분 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ Seller(id: Long, userId: String, passwordHash: String, name: String, notificationEnabled: boolean, businessName: String, businessOwnerName: String, businessNumber: String, businessCategory: String)` | 판매자 객체를 생성하고 사용자 및 사업자 정보를 설정 |
| `+ getBusinessName(): String` | 사업장 이름을 반환 |
| `+ getBusinessOwnerName(): String` | 사업자 대표자 이름을 반환 |
| `+ getBusinessNumber(): String` | 사업자 등록번호를 반환 |
| `+ getBusinessCategory(): String` | 사업장 카테고리를 반환 |
| `+ registerSaleInfo(sale: Sale): void` | 판매자가 할인 정보를 등록 |

### 4) Food

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- id: Long` | 식료품을 구분하는 고유 번호 |
| `- itemName: String` | 식료품 이름 |
| `- category: String` | 식료품의 대분류 |
| `- subcategory: String` | 식료품의 세부 분류 |
| `- quantity: Integer` | 식료품 수량 |
| `- unit: String` | 식료품 단위 |
| `- expiryDate: LocalDate` | 식료품 소비기한 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ Food(id: Long, itemName: String, category: String, subcategory: String, quantity: Integer, unit: String, expiryDate: LocalDate)` | 식료품 객체를 생성하고 식료품 정보를 설정 |
| `+ getId(): Long` | 식료품 고유 번호를 반환 |
| `+ getItemName(): String` | 식료품 이름을 반환 |
| `+ getCategory(): String` | 식료품 대분류를 반환 |
| `+ getSubcategory(): String` | 식료품 세부 분류를 반환 |
| `+ getQuantity(): Integer` | 식료품 수량을 반환 |
| `+ getUnit(): String` | 식료품 단위를 반환 |
| `+ getExpiryDate(): LocalDate` | 식료품 소비기한을 반환 |
| `+ updateQuantity(quantity: Integer): void` | 식료품 수량을 수정 |
| `+ updateExpiryDate(expiryDate: LocalDate): void` | 식료품 소비기한을 수정 |
| `+ calculateDaysUntillExpiry(today: LocalDate) : Integer `| 기준날짜를 바탕으로 소비기한이 얼마나 남았는지 계산함
| `+ isExpired(today: LocalDate): boolean` | 기준 날짜를 바탕으로 소비기한 경과 여부를 확인 |

### 5) Sale

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- id: Long` | 할인 정보를 구분하는 고유 번호 |
| `- originalPrice: BigDecimal` | 할인 전 정가 |
| `- salePrice: BigDecimal` | 할인 적용 가격 |
| `- startDate: LocalDate` | 할인 시작일 |
| `- endDate: LocalDate` | 할인 종료일 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ Sale(id: Long, originalPrice: BigDecimal, salePrice: BigDecimal, startDate: LocalDate, endDate: LocalDate)` | 할인 정보 객체를 생성하고 가격 및 기간 정보를 설정 |
| `+ getId(): Long` | 할인 고유 정보 반환 |
| `+ getOriginalPrice(): BigDecimal` | 정가를 반환 |
| `+ getSalePrice(): BigDecimal` | 할인가를 반환 |
| `+ getStartDate(): LocalDate` | 할인 시작일을 반환 |
| `+ getEndDate(): LocalDate` | 할인 종료일을 반환 |
| `+ calculateDiscountRate(): Integer` | 정가와 할인가를 기준으로 할인율을 계산 |
| `+ isActive(today: LocalDate): boolean` | 기준 날짜에 할인이 진행 중인지 확인 |

### 6) Address

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- id: Long` | 주소 정보를 구분하는 고유 번호 |
| `- postcode: String` | 우편번호 |
| `- address: String` | 기본 주소 |
| `- detailAddress: String` | 상세 주소 |
| `- defaultAddress: boolean` | 기본 주소 여부 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ Address(id: Long, postcode: String, address: String, detailAddress: String, defaultAddress: boolean)` | 주소 객체를 생성하고 주소 정보를 설정 |
| `+ getPostcode(): String` | 우편번호를 반환 |
| `+ getAddress(): String` | 기본 주소를 반환 |
| `+ getDetailAddress(): String` | 상세 주소를 반환 |
| `+ isDefaultAddress(): boolean` | 기본 주소 여부를 반환 |
| `+ changeDetailAddress(detailAddress: String): void` | 상세 주소를 변경 |
| `+ extractRegion(): String` | 주소에서 광역시 또는 시 단위 지역명을 추출 |

### 7) Recipe

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- id: String` | 레시피를 구분하는 고유 값 |
| `- name: String` | 레시피 이름 |
| `- category: String` | 레시피 분류 |
| `- ingredients: List<String>` | 레시피에 필요한 재료 목록 |
| `- instructions: List<String>` | 조리 순서 목록 |
| `- difficulty: String` | 조리 난이도 |
| `- cookingTime: String` | 예상 조리 시간 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ Recipe(id: String, name: String, category: String, ingredients: List<String>, instructions: List<String>, difficulty: String, cookingTime: String)` | 레시피 객체를 생성하고 레시피 정보를 설정 |
| `+ getName(): String` | 레시피 이름을 반환 |
| `+ getIngredients(): List<String>` | 필요한 재료 목록을 반환 |
| `+ getInstructions(): List<String>` | 조리 순서 목록을 반환 |
| `+ getDifficulty(): String` | 조리 난이도를 반환 |
| `+ getCookingTime(): String` | 예상 조리 시간을 반환 |
| `+ findMissingIngredients(foods: List<Food>): List<String>` | 보유 식료품과 비교하여 부족한 재료를 계산 |

### 8) Notification

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- id: Long` | 알림을 구분하는 고유 번호 |
| `- userid: Long`| 유저를 구분하는 번호 |
| `- title: String` | 알림 제목 |
| `- message: String` | 알림 내용 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ Notification(id: Long, userid: Long,title: String, message: String)` | 알림 객체를 생성하고 알림 정보를 설정 |


### 9) NotificationSetting

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- enabled: boolean` | 전체 알림 사용 여부 |
| `- notify1day: boolean` | 소비기한 1일 전 알림 사용 여부 |
| `- notify3day: boolean` | 소비기한 3일 전 알림 사용 여부 |
| `- notify7day: boolean` | 소비기한 7일 전 알림 사용 여부 |
| `- discountEnabled: boolean` | 주변 할인 알림 사용 여부 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ NotificationSetting(enabled: boolean, notify1day: boolean, notify3day: boolean, notify7day: boolean, discountEnabled: boolean)` | 알림 설정 객체를 생성하고 설정 값을 초기화 |
| `+ isEnabled(): boolean` | 전체 알림 사용 여부를 반환 |
| `+ isNotify1day(): boolean` | 1일 전 알림 사용 여부를 반환 |
| `+ isNotify3day(): boolean` | 3일 전 알림 사용 여부를 반환 |
| `+ isNotify7day(): boolean` | 7일 전 알림 사용 여부를 반환 |
| `+ isDiscountEnabled(): boolean` | 할인 알림 사용 여부를 반환 |
| `+ enableNotification(): void` | 전체 알림을 활성화 |
| `+ disableNotification(): void` | 전체 알림을 비활성화 |
| `+ changeExpiryDays(notify1day: boolean, notify3day: boolean, notify7day: boolean): void` | 소비기한 알림 기준일 설정을 변경 |
| `+ changeDiscountNotification(discountEnabled: boolean): void` | 할인 알림 사용 여부를 변경 |

<br>
<img width=auto height=auto alt="image" src="image/Class diagram/Class Diagram2.png"/>
<br><br>

### 10) ApiController

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- authService: AuthService` | 인증 및 사용자 관련 요청을 처리하기 위한 서비스 |
| `- foodService: FoodService` | 식료품 관련 요청을 처리하기 위한 서비스 |
| `- recipeClient: RecipeClient` | 외부 레시피 API를 호출하기 위한 클라이언트 |
| `- saleService: SaleService` | 할인 및 알림 관련 요청을 처리하기 위한 서비스 |
| `- userRepository: UserRepository` | 사용자 역할 확인 등 사용자 데이터 조회를 위한 저장소 |
| `- jwtService: JwtService` | 요청의 인증 토큰을 검증하기 위한 서비스 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ ApiController(authService: AuthService,foodService: FoodService,recipeClient: RecipeClient,saleService: SaleService,userRepository: UserRepository,jwtService: JwtService)` | 컨트롤러에서 사용할 서비스와 저장소 의존성을 주입 |
| `+ login(request: LoginRequest): AuthResponse` | 로그인 요청을 처리 |
| `+ signup(request: SignupRequest, role: String): AuthResponse` | 사용자 회원가입 요청을 처리 |
| `+ logout(authorization: String): void` | 로그인된 사용자의 로그아웃 요청을 처리 |
| `+ saveMyAddress(authorization: String, request: AddressRequest): AddressResponse` | 로그인한 사용자의 주소 저장 요청을 처리 |
| `+ saveNotificationSettings(authorization: String, request: NotificationSettingsRequest): NotificationSettingsResponse` | 로그인한 사용자의 알림 설정 저장 요청을 처리 |
| `+ foods(authorization: String): List<FoodResponse>` | 로그인한 사용자의 식료품 목록 조회 요청을 처리 |
| `+ createFood(authorization: String, request: FoodCreateRequest): FoodResponse` | 식료품 등록 요청을 처리 |
| `+ updateFood(authorization: String, id: Long, request: FoodCreateRequest): FoodResponse` | 식료품 수정 요청을 처리 |
| `+ deleteFood(authorization: String, id: Long): void` | 식료품 삭제 요청을 처리 |
| `+ sales(authorization: String): List<SaleResponse>` | 판매자 할인 목록 또는 소비자 주변 할인 목록 조회 요청을 처리 |
| `+ createSale(authorization: String, request: SaleCreateRequest): SaleResponse` | 할인 정보 등록 요청을 처리 |
| `+ deleteSale(authorization: String, id: Long): void` | 할인 정보 삭제 요청을 처리 |
| `+ createExpiryNotifications(authorization: String, request: ExpiryNotificationRequest): List<NotificationResponse>` | 소비기한 알림 생성 요청을 처리 |
| `+ recipes(authorization: String): List<RecipeResponse>` | 레시피 검색 요청을 처리 |
| `+ checkBusinessNumber(in businessNumber:String): CheckBusinessNumberResponse` | 판매자 회원가입 중 사업자 번호 검증 요청을 받아 인증 서비스에 전달하고 검증 결과를 반환 |
| `+ enterMarketInfo(in request:MarketInfoRequest): MarketInfoResponse`| 판매자 회원가입 중 사업장 정보 입력 요청을 받아 인증 서비스에 전달하고 처리 결과를 반환 |

### 11) AuthService

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- userRepository: UserRepository` | 사용자 정보 조회 및 저장을 담당하는 저장소 |
| `- jwtService: JwtService` | 로그인 성공 시 토큰을 생성하기 위한 서비스 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ AuthService(userRepository: UserRepository, jwtService: JwtService)` | 인증 서비스에 필요한 저장소와 보안 관련 의존성을 주입 |
| `+ login(request: LoginRequest): AuthResponse` | 아이디와 비밀번호를 검증하고 인증 응답을 생성 |
| `+ signup(request: SignupRequest, role: String): AuthResponse` | 사용자 유형에 따라 소비자 또는 판매자 계정을 생성 |
| `+ saveNotificationSettings(userId: Long, request: NotificationSettingsRequest): NotificationSettingsResponse` | 사용자의 알림 설정을 저장하고 결과를 반환 |
| `+ saveAddress(userId: Long, request: AddressRequest): AddressResponse` | 사용자의 기본 주소를 저장하고 결과를 반환 |
| `+ checkBusinessNumber(in businessNumber:String): CheckBusinessNumberResponse` | 사업자 번호의 형식과 중복 여부를 검증하여 사용가능 여부를 판단 |
| `+ enterMarketInfo(in request:MarketInfoRequest): MarketInfoResponse`| 입력된 사업장 정보를 기반으로 판매자 도메인 정보를 구성하고 사업장 정보 응답을 생성 |
### 12) FoodService

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- foodRepository: FoodRepository` | 식료품 데이터 조회 및 저장을 담당하는 저장소 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ FoodService(foodRepository: FoodRepository)` | 식료품 서비스에서 사용할 저장소 의존성을 주입 |
| `+ findAll(ownerId: Long): List<FoodResponse>` | 사용자가 등록한 식료품 목록을 조회 |
| `+ create(sellerId: Long, request: FoodCreateRequest): FoodResponse` | 식료품을 등록하고 응답 객체로 반환 |
| `+ update(id: Long, sellerId: Long, request: FoodCreateRequest): FoodResponse` | 식료품 정보를 수정하고 응답 객체로 반환 |
| `+ delete(id: Long, sellerId: Long): void` | 사용자가 등록한 식료품을 삭제 |
| `+ createExpiryNotifications(userId: Long, request: ExpiryNotificationRequest): List<NotificationResponse>` | 소비기한 기준에 해당하는 식료품 알림을 생성 |

### 13) SaleService

|**Attributes**| |
|---|---|
| 속성 | 설명 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ findAll(sellerId: Long): List<SaleResponse>` | 판매자가 등록한 할인 정보 목록을 조회 |
| `+ findNearby(region: String): List<SaleResponse>` | 사용자의 지역 기준으로 주변 할인 정보를 조회 |
| `+ create(sellerId: Long, request: SaleCreateRequest): SaleResponse` | 할인 정보를 등록하고 주변 소비자에게 알림 기록을 생성 |
| `+ delete(sellerId: Long, id: Long): void` | 판매자가 등록한 할인 정보를 삭제 |
| `- validateSaleRequest(SellerId: Long,request: SaleCreateRequest): void` | 할인 정보가 유효한지 검사(할인가가 정가보다 낮은지 확인, 할인 시작일이 현재날짜보다 과거가 아닌지 확인,할인 종료일이 할인시작일보다 빠른지 확인,같은 품목이 이미 등록되어있는지 확인함) |
| `- notifyNearbyConsumers(sellerId: Long, sale: SaleResponse): void` | 할인 등록 시 주변 소비자에게 할인 알림 기록을 생성 |

### 14) RecipeClient

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- apiKey: String` | 식품안전나라 레시피 API 호출에 사용하는 인증 키 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ RecipeClient(apiKey: String)` | 레시피 API 호출에 필요한 API 키를 설정 |
| `+ search(query: String): List<Recipe>` | 검색어를 기준으로 외부 레시피 API를 호출하고 결과를 반환 |

### 15) JwtService

|**Attributes**| |
|---|---|
| 속성 | 설명 |
| `- secret: byte[]` | JWT 서명 생성 및 검증에 사용하는 비밀 키 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ JwtService(secret: String)` | 문자열 비밀 키를 바이트 배열로 변환하여 저장 |
| `+ createToken(user: User): String` | 사용자 정보를 바탕으로 JWT 토큰을 생성 |
| `+ requireUserId(authorizationHeader: String): Long` | Authorization 헤더에서 토큰을 검증하고 사용자 번호를 추출 |

### 16) UserRepository

|**Attributes**| |
|---|---|
| 속성 | 설명 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ findByUserId(userId: String): Optional<User>` | 로그인 아이디로 사용자를 조회 |
| `+ existsByUserId(userId: String): boolean` | 이미 사용 중인 아이디인지 확인 |
| `+ existsByBusinessNumber(businessNumber: String): boolean` | 이미 등록된 사업자번호인지 확인 |
| `+ createUser(request: SignupRequest, role: String, passwordHash: String): User` | 회원가입 정보를 바탕으로 사용자 정보를 저장 |
| `+ findDefaultAddress(userId: Long): Optional<AddressResponse>` | 사용자의 기본 주소를 조회 |
| `+ saveNotificationSettings(userId: Long, request: NotificationSettingsRequest): void` | 사용자의 알림 설정을 저장 |
| `+ saveDefaultAddress(userId: Long, request: AddressRequest): void` | 사용자의 기본 주소를 저장 |
| `+ createSellerInfo(userId: Long, request: SignupRequest): void` | 판매자 사업자 정보를 저장 |

### 17) FoodRepository

|**Attributes**| |
|---|---|
| 속성 | 설명 |

|**Methods**| |
|---|---|
| Operation | 설명 |
| `+ findByOwner(ownerId: Long): List<Food>` | 사용자 고유 번호를 기준으로 식료품 목록을 조회 |
| `+ findOwned(ownerId: Long, foodId: Long): Food` | 특정 사용자가 소유한 식료품을 조회 |
| `+ create(ownerId: Long, request: FoodCreateRequest): Food` | 식료품 정보를 저장하고 생성된 식료품 객체를 반환 |
| `+ update(ownerId: Long, foodId: Long, request: FoodCreateRequest): Food` | 식료품 정보를 수정하고 수정된 식료품 객체를 반환 |
| `+ delete(ownerId: Long, foodId: Long): void` | 사용자가 소유한 식료품을 삭제 |

</div>
</span>

<br><br><br><br><br><br><br><br><br><br>

<h2>
3. Sequence Diagram
</h2>

<div style="line-height: 1.6;">
<span style="font-size: 12pt;">

본 장에서는 Analysis 문서에서 정의한 Use Case를 기준으로 Food Manager의 주요 객체 간 메시지 흐름을 Sequence Diagram으로 표현한다. 각 Sequence Diagram은 하나의 Use Case를 중심으로 작성하며, 공통적으로 Actor가 첫 메시지를 보내고 `ApiController`가 요청을 수신한 뒤 필요한 Service, Repository, Domain 객체와 협력하는 구조를 가진다.  
입력 형식 오류나 네트워크 통신 오류처럼 화면 또는 외부 환경에서 처리되는 오류는 시퀀스 다이어그램의 핵심 객체 책임에서 제외하고, 도메인 검증과 데이터 조회 결과에 따라 발생하는 예외 흐름은 `alt`, `opt`, `loop`, `ref` fragment로 표현한다.

<br>

3.1 Register Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Register Sequence diagram.png"/>
<br>

Register Sequence Diagram은 사용자가 소비자 또는 판매자 계정으로 회원가입하는 흐름을 나타낸다. 사용자가 `signup(request, role)` 요청을 보내면 `ApiController`는 `AuthService`에 회원가입 처리를 위임하고, `AuthService`는 `UserRepository`를 통해 아이디 중복 여부를 확인한다. 이미 사용 중인 아이디이면 오류 응답을 반환하고, 사용 가능한 아이디이면 `createUser(request, role, passwordHash)`를 통해 사용자를 생성한다. 이후 역할에 따라 `Consumer` 또는 `Seller` 객체가 생성되며, 판매자 회원가입의 경우 사업장 정보 등록 흐름인 Add Market을 `ref` fragment로 참조한다. 회원가입이 완료되면 `JwtService`가 토큰을 생성하고 인증 응답을 반환한다.
<br>
3.2 Login Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Login Sequence diagram.png"/>
<br>

Login Sequence Diagram은 사용자가 아이디와 비밀번호로 로그인하는 흐름을 나타낸다. `ApiController`는 로그인 요청을 `AuthService`로 전달하고, `AuthService`는 `UserRepository.findByUserId(userId)`를 통해 사용자를 조회한다. 사용자가 존재하지 않거나 비밀번호가 일치하지 않으면 동일하게 로그인 실패 메시지를 반환한다. 사용자가 존재하고 비밀번호가 일치하면 `User`의 역할 정보를 확인한 뒤 `JwtService.createToken(user)`으로 JWT 토큰을 생성하여 `AuthResponse`를 반환한다.
<br>
3.3 Logout Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Logout Sequence diagram.png"/>
<br>

Logout Sequence Diagram은 사용자가 현재 로그인 세션을 종료하는 흐름을 나타낸다. 사용자가 `logout(authorization)` 요청을 보내면 `ApiController`는 `JwtService.requireUserId(authorization)`를 통해 현재 사용자를 확인하고 로그아웃 완료 응답을 반환한다. 실제 토큰 폐기나 화면 상태 초기화는 클라이언트에서 처리되지만, 설계 관점에서는 인증된 사용자가 로그아웃 요청을 수행하는 흐름으로 표현한다.
<br>
3.4 Register Region Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Register Region Sequence diagram.png"/>
<br>

Register Region Sequence Diagram은 사용자가 주소를 등록하거나 변경하는 흐름을 나타낸다. 사용자가 우편번호와 기본 주소를 선택하면 `Address` 객체가 생성되고, 상세 주소는 `changeDetailAddress(detailAddress)`로 반영된다. 이후 `saveMyAddress(authorization, request)` 요청이 `ApiController`로 전달되고, `AuthService.saveAddress(userId, request)`가 `UserRepository.saveDefaultAddress(userId, request)`를 호출하여 주소를 저장한다. 신규 주소인 경우 `User.registerAddress(address)`를 호출하고, 기존 주소를 변경하는 경우 `User.updateAddress(address)`를 호출한다. 마지막으로 저장된 주소의 우편번호, 기본 주소, 상세 주소, 기본 주소 여부를 조회하여 `AddressResponse`를 반환한다.
<br>
3.5 Set Notification Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Set Notification Sequence diagram.png"/>
<br>

Set Notification Sequence Diagram은 사용자가 소비기한 알림과 할인 알림 설정을 변경하는 흐름을 나타낸다. 사용자가 `saveNotificationSettings(authorization, request)`를 호출하면 `ApiController`는 인증 정보를 확인한 뒤 `AuthService.saveNotificationSettings(userId, request)`에 처리를 위임한다. `NotificationSetting` 객체는 전체 알림 사용 여부를 확인하고, 알림이 비활성화된 경우 소비기한 알림 기준일과 할인 알림을 모두 false로 변경한다. 알림이 활성화된 경우 1일 전, 3일 전, 7일 전 알림과 할인 알림 설정을 사용자가 선택한 값으로 변경한다. 변경된 설정은 `UserRepository.saveNotificationSettings(userId, request)`로 저장되고 `User.setNotification(setting)`을 통해 사용자 객체에 반영된다.
<br>
3.6 Add Food Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Add Food Sequence diagram.png"/>
<br>

Add Food Sequence Diagram은 사용자가 식료품 정보를 등록하는 흐름을 나타낸다. 사용자가 `createFood(authorization, request)`를 호출하면 `ApiController`는 `JwtService`를 통해 사용자 번호를 확인하고 `FoodService.create(userId, request)`에 등록을 위임한다. `FoodService`는 수량이 1보다 작은 경우와 소비기한이 현재 날짜보다 이전인 경우를 예외 흐름으로 처리한다. 유효한 요청이면 `FoodRepository.create(userId, request)`를 통해 `Food` 객체를 생성하고, `User.addFood(food)`를 통해 사용자의 식료품 목록에 등록한다. 이후 식료품 이름, 카테고리, 수량, 단위 등을 조회하여 `FoodResponse`를 반환한다.

<br>
3.7 Check Expiry Date Sequence diagram
<br>

Check Expiry Date Sequence Diagram은 사용자가 등록한 식료품의 소비기한을 확인하고 알림을 생성하는 흐름을 나타낸다. 사용자가 `createExpiryNotifications(authorization, request)`를 호출하면 `FoodService`는 `FoodRepository.findByOwner(userId)`로 사용자의 식료품 목록을 조회한다. 등록된 식료품이 없으면 빈 알림 목록을 반환한다. 식료품이 존재하면 각 `Food`에 대해 `getExpiryDate()`, `calculateDaysUntilExpiry(today)`, `isExpired(today)`를 호출하여 소비기한 상태를 확인한다. 설정된 알림 기준일 이내에 해당하는 식료품이 있으면 `Notification` 객체를 생성하고 `User.receiveNotification(notification)`으로 알림을 전달한다.
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Check Expiry Date Sequence diagram.png"/>
<br><br>
3.8 Delete Food Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Delete Food Sequence diagram.png"/>
<br>

Delete Food Sequence Diagram은 사용자가 등록된 식료품을 선택하여 삭제하는 흐름을 나타낸다. 먼저 `foods(authorization)` 요청으로 사용자의 식료품 목록을 조회하고, 목록이 없으면 등록된 식료품이 없다는 메시지를 반환한다. 목록이 존재하면 각 식료품의 `getId()`와 `getItemName()`을 통해 삭제 대상 선택 정보를 표시한다. 사용자가 삭제할 항목을 선택하면 `deleteFood(authorization, id)` 요청이 전달되고, `FoodService.delete(id, userId)`는 `FoodRepository.findOwned(userId, id)`로 삭제 대상 식료품을 확인한 뒤 `User.deleteFood(foodId)`와 `FoodRepository.delete(userId, id)`를 통해 삭제를 수행한다.
<br>
3.9 Update Food Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Update Food Sequence diagram.png"/>
<br>

Update Food Sequence Diagram은 사용자가 기존 식료품 정보를 수정하는 흐름을 나타낸다. 사용자가 `updateFood(authorization, id, request)`를 호출하면 `ApiController`는 인증된 사용자 번호를 확인하고 `FoodService.update(id, userId, request)`에 수정을 위임한다. `FoodService`는 `FoodRepository.findOwned(userId, id)`로 수정 대상 식료품을 조회한 후, 수량이 1보다 작은 경우와 소비기한이 현재 날짜보다 이전인 경우를 예외 흐름으로 처리한다. 유효한 요청이면 `Food.updateQuantity(quantity)`와 `Food.updateExpiryDate(expiryDate)`로 도메인 객체의 값을 변경하고, `FoodRepository.update(userId, id, request)` 및 `User.updateFood(food)`를 통해 수정 결과를 반영한다.
<br>
3.10 Search Food Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Search Food Sequence diagram.png"/>
<br>

Search Food Sequence Diagram은 사용자가 자신이 등록한 식료품 목록에서 조건에 맞는 식료품을 조회하는 흐름을 나타낸다. 사용자가 `foods(authorization)`를 호출하면 `FoodService.findAll(userId)`가 `FoodRepository.findByOwner(userId)`를 통해 사용자의 식료품 목록을 가져온다. `User.viewFoods()`로 사용자의 식료품 목록 조회 행위를 표현하고, 각 식료품의 이름, 카테고리, 세부 카테고리를 비교한다. 검색 조건에 맞는 식료품이 없으면 검색 결과 없음 메시지를 반환하고, 조건에 맞는 식료품이 있으면 `List<FoodResponse>`를 반환한다.
<br>
3.11 Check Sale Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Check Sale Sequence diagram.png"/>
<br>

Check Sale Sequence Diagram은 소비자가 자신의 주소를 기준으로 주변 할인 정보를 조회하는 흐름을 나타낸다. 소비자가 `sales(authorization)`를 호출하면 `ApiController`는 사용자 번호를 확인하고 `UserRepository.findDefaultAddress(userId)`로 기본 주소를 조회한다. 주소가 등록되어 있지 않으면 위치 등록이 필요하다는 메시지를 반환한다. 주소가 존재하면 `Address.extractRegion()`으로 지역 정보를 추출하고, `SaleService.findNearby(region)`을 통해 같은 광역시 또는 시 단위의 할인 목록을 조회한다. 주변 할인 정보가 없으면 주변 할인 정보 없음 메시지를 반환하고, 할인 정보가 존재하면 각 `Sale`의 활성 여부와 할인율을 확인한 뒤 `Consumer.viewSaleInfo(sales)`를 통해 소비자에게 할인 목록을 제공한다.
<br>
3.12 Check recipe Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Check Recipe Sequence diagram.png"/>
<br>

Check Recipe Sequence Diagram은 소비자가 보유 식료품을 기준으로 만들 수 있는 레시피를 추천받는 흐름을 나타낸다. 소비자가 `recipes(authorization)`를 호출하면 `ApiController`는 사용자 번호를 확인하고 `FoodService.findAll(userId)`를 통해 등록된 식료품 목록을 조회한다. 등록된 식료품이 없으면 레시피 추천에 사용할 식료품이 없다는 메시지를 반환한다. 식료품이 존재하면 `Consumer.recommendRecipe(foods)`가 호출되고, `RecipeClient.search(query)`를 통해 레시피 데이터를 조회한다. 생성된 `Recipe` 객체는 이름, 재료, 조리 순서, 난이도, 조리 시간을 제공하며, `findMissingIngredients(foods)`로 추가 구매가 필요한 재료를 계산한다.
<br>
3.13 Add Market Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Add Market Sequence diagram.png"/>
<br>

Add Market Sequence Diagram은 판매자 회원가입 과정에서 사업장 정보를 입력하고 검증하는 흐름을 나타낸다. 판매자가 사업자번호를 입력하면 `checkBusinessNumber(businessNumber)` 요청이 `ApiController`와 `AuthService`로 전달된다. 사업자번호 형식이 올바르지 않으면 형식 오류 메시지를 반환하고, 형식이 올바른 경우 `UserRepository.existsByBusinessNumber(businessNumber)`로 중복 등록 여부를 확인한다. 이미 등록된 사업자번호이면 중복 오류를 반환하고, 사용 가능한 사업자번호이면 판매자가 사업장 이름, 사업자 이름, 사업장 카테고리 등을 입력한다. 입력된 정보는 `Seller` 생성자와 getter를 통해 확인되며, 사업장 주소 입력은 Register Region 흐름을 `ref` fragment로 참조한다.
<br>
3.14 Add Sale Information Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Add Sale Information Sequence diagram.png"/>
<br>

Add Sale Information Sequence Diagram은 판매자가 자신이 등록한 식료품을 기준으로 할인 정보를 등록하는 흐름을 나타낸다. 먼저 판매자는 `foods(authorization)` 요청을 통해 자신의 식료품 목록을 조회한다. 등록된 식료품이 없으면 할인 등록을 진행할 수 없다는 메시지를 반환한다. 식료품이 존재하면 판매자는 특정 식료품을 선택하여 `createSale(authorization, request)`를 호출한다. `SaleService`는 선택한 식료품을 `FoodRepository.findOwned(sellerId, foodId)`로 확인하고, 할인가가 정가보다 낮은지, 할인 시작일이 현재 날짜 이후인지, 종료일이 시작일 이후인지, 재고 수량이 남아 있는지, 같은 식료품에 대한 할인 정보가 이미 존재하지 않는지 검증한다. 검증을 통과하면 `Sale` 객체를 생성하고 `calculateDiscountRate()`로 할인율을 계산한 뒤 `Seller.registerSaleInfo(sale)`로 할인 정보를 등록한다. 이후 `notifyNearbyConsumers(sellerId, sale)`를 통해 주변 소비자에게 `Notification`을 생성하고 `User.receiveNotification(notification)`으로 할인 알림을 전달한다.
<br>
3.15 Delete Sale Information Sequence diagram
<br>

Delete Sale Information Sequence Diagram은 판매자가 등록한 할인 정보를 선택하여 삭제하는 흐름을 나타낸다. 먼저 `sales(authorization)` 요청을 통해 판매자가 등록한 할인 목록을 조회한다. 할인 정보가 없으면 등록된 할인 정보가 없다는 메시지를 반환한다. 할인 정보가 존재하면 각 할인 정보의 `getId()`와 `getSalePrice()`를 통해 삭제 대상 선택 정보를 표시한다. 판매자가 삭제할 할인 정보를 선택하면 `deleteSale(authorization, id)` 요청이 전달되고, `SaleService.delete(sellerId, id)`가 해당 판매자의 할인 정보를 삭제한다. 삭제가 완료되면 OK 응답을 반환한다.
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Delete Sale Information Sequence diagram.png"/>
<br>

3.16 Check Sale Information Sequence diagram
<br>
<img width=auto height=auto alt="image" src="image/sequence diagram/Check Sale Information Sequence diagram.png"/>
<br>

Check Sale Information Sequence Diagram은 판매자가 자신이 등록한 할인 정보를 조회하는 흐름을 나타낸다. 판매자가 `sales(authorization)`를 호출하면 `ApiController`는 `JwtService.requireUserId(authorization)`로 판매자 번호를 확인하고 `SaleService.findAll(sellerId)`를 호출한다. 등록된 할인 정보가 없으면 등록된 할인 정보가 없다는 메시지를 반환한다. 할인 정보가 존재하면 각 `Sale`에 대해 정가, 할인가, 할인율, 시작일, 종료일, 현재 활성 여부를 조회한다. 조회된 값은 `List<SaleResponse>` 형태로 정리되어 판매자에게 반환되며, 판매자는 이를 통해 현재 등록된 할인 정보의 상세 내용을 확인할 수 있다.
<br>


<br><br><br><br><br><br><br><br><br>
<h2>
4. State Machine Diagram
</h2>

<div style="line-height: 1.6;">
<span style="font-size: 12pt;">

<img width=auto height=auto alt="image" src="image/State Machine diagram/State Machine Sequence diagram.png"/>

1. unauthenticated
앱이 실행되면 Welcome 상태로 진입한다. 사용자는 로그인 화면인 Login 상태 또는 회원가입을 위한 Register 복합상태로 이동할 수 있다. 로그인 실패 시 Login 상태에 머무르고, 로그인 성공 시 역할에 따라 소비자 또는 판매자 상태로 이동한다.
Register 내부에서는 소비자 회원가입과 판매자 회원가입을 구분하며, 판매자는 추가로 Add Market 상태에서 사업장 정보를 등록한다. 회원가입이 완료되면 바로 로그인되는 것이 아니라 RegisterComplete를 거쳐 다시 Login 상태로 이동한다.

2. Consumer
Consumer 상태는 소비자 계정으로 로그인한 사용자의 상태이다. 소비자는 ConsumerMain에서 식료품 관리, 할인 조회, 레시피 추천, 설정 기능으로 이동할 수 있다. 할인 조회 시 주소가 등록되어 있지 않으면 주소 등록 상태로 이동하고, 레시피 추천 시 추천 결과가 있으면 레시피 조회 상태로 이동하며 결과가 없으면 NoRecipe 상태가 된다.

3. Seller
Seller 상태는 판매자 계정으로 로그인한 사용자의 상태이다. 판매자는 SellerMain에서 식료품 관리, 할인 정보 관리, 설정 기능으로 이동할 수 있다. Sale Management에서는 할인 정보를 조회하고, 할인 추가 또는 삭제 상태로 전이된다.

4. Food
Food 상태는 소비자와 판매자가 공통으로 사용하는 식료품 관리 상태이다. Food main에서 식료품 추가, 수정, 삭제, 검색, 소비기한 확인 상태로 이동한다. 각 작업이 완료되면 다시 이전 사용자 상태로 돌아간다.

5. Setting
Setting 상태는 소비자와 판매자가 공통으로 사용하는 설정 관리 상태이다. Setting Main에서 알림 설정 상태 또는 주소 등록 상태로 이동한다. 설정이나 주소 저장이 완료되면 다시 Setting Main으로 돌아간다. 판매자는 주소가 필수이므로 주소 삭제는 제한된다.

6. 종료
소비자 또는 판매자 상태에서 로그아웃하면 다시 unauthenticated 상태로 이동한다. 앱을 종료하면 최종 상태로 전이된다.

</div>
</span>

<br><br><br><br><br><br><br><br><br>
<h2>
5. Implementation requirements
</h2>

<div style="line-height: 1.6;">
<span style="font-size: 12pt;">

5.1 HW Requirements

Food Manager 앱을 개발하고 실행하기 위한 하드웨어 요구사항은 다음과 같다.

| 항목 | 요구사항 |
|---|---|
| CPU | Intel i5 이상 또는 동급 CPU |
| Memory | 8GB RAM 이상 |
| Storage | 최소 10GB 이상의 여유 공간 |

5.2 SW Requirements

Food Manager 앱을 개발하고 실행하기 위한 소프트웨어 요구사항은 다음과 같다.

| 항목 | 요구사항 |
|---|---|
| Operating System | Windows 10/11 |
| Development Tool | Visual Studio Code |
| Runtime | Node.js, Java 17 |
| Framework | React, Vite, Spring Boot, Expo |
| Database | PostgreSQL |
| Container | Docker Desktop |
| Build Tool | npm |
| External API | Kakao Postcode Service, 식품안전나라 레시피 API |

5.3 Android Requirements

Food Manager 모바일 앱을 Android 환경에서 실행하기 위한 요구사항은 다음과 같다.

| 항목 | 요구사항 |
|---|---|
| Android Version | Android 8.0 이상 |
| APK Format | Android 기기에 설치 가능한 APK 파일 |

</div>
</span>

<h2>
6. Glossary
</h2>

<div style="line-height: 1.6;">
<span style="font-size: 12pt;">

| Term | Description |
|---|---|
| Food Manager | 식료품, 할인, 레시피, 알림을 관리하는 앱 |
| Method | 클래스에 구현된 함수들을 Method라 칭한다.|
| Attribute | 객체의 상태를 나타내며, 데이터 또는 속성이라고 함|
| Class Diagram | “Class”라고 하는 객체지향 설계단위를 이용하여 시스템의 정적인
structure (구조)를 표현한 다이어그램 |
| Sequence Diagram | 객체간의 동적 상호작용을 시간적 개념을 중심으로 모델링하는 과정|
| State Machine Diagram | 객체 lifetime동안의 변환될 수 있는 모든 상태를 정의해둔 Diagram |

</div>
</span>


<h2>
7. References
</h2>

<div style="line-height: 1.6;">
<span style="font-size: 12pt;">

| No. | Reference |
|---|---|
| 1 | Spring Boot Documentation, https://docs.spring.io/spring-boot/ |
| 2 | React Documentation, https://react.dev/ |
| 3 | PostgreSQL Documentation, https://www.postgresql.org/docs/ |
| 4 | Expo Documentation, https://docs.expo.dev/ |
| 5 | Kakao Postcode Service Guide, https://postcode.map.kakao.com/guide |
| 6 | 식품안전나라 조리식품 레시피 API, https://www.foodsafetykorea.go.kr/api/openApiInfo.do?svc_no=COOKRCP01 |
| 7 | StarUML Documentation, https://docs.staruml.io/ |

</div>
</span>